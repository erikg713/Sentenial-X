aiohttp>=3.8.0
aiosqlite>=0.17.0
scikit-learn>=1.2.0
python-decouple>=3.6.0
typer[all]
requests
# basic
pip install torch torchvision

# RL libraries
pip install stable-baselines3[extra]   # PPO, A2C implementations
pip install gym
pip install pettingzoo                 # multi-agent envs
pip install rllib                     # optional: Ray RLlib for scalable MARL (pip install 'ray[rllib]')

# telemetry / infra
pip install confluent-kafka           # Kafka client
pip install sqlalchemy psycopg2-binary
pip install pandas numpy

# optional advanced
pip install gymnasium                 # modern gym replacement if needed
pip install wandb                     # experiment logging
